import { Container, Typography } from "@mui/material";

export default function Range() {
  return (
    <Container maxWidth="lg" className="py-8">
      <Typography variant="h4">Range View</Typography>
      {/* Add range selection and display logic here */}
    </Container>
  );
}
